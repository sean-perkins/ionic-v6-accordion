import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'accordion-v6-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
